import React, { useState } from "react";
import { Card, CardContent, Typography } from "@mui/material";

const View = () => {
  return (
    <Typography variant="body2">
      This is the default view. Click on a user to view their clips!
    </Typography>
  );
};

export default View;
